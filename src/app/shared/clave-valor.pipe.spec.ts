import { ClaveValorPipe } from './clave-valor.pipe';

describe('ClaveValorPipe', () => {
  it('create an instance', () => {
    const pipe = new ClaveValorPipe();
    expect(pipe).toBeTruthy();
  });
});
